# AGENTS.md

## Cursor Cloud specific instructions

This repository (`ai-game`) is currently an empty scaffold with only a `README.md`. There are no applications, services, dependencies, tests, or build systems to run.

When application code is added, update this section with:
- How to install dependencies and which package manager to use.
- How to run, build, lint, and test the application.
- Any non-obvious startup caveats or environment requirements.
